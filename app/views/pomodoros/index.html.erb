<p style="color: green"><%= notice %></p>

<h1>Pomodoros</h1>

<%= link_to "New pomodoro", new_pomodoro_path %>

<div id="pomodoros">
  <% @pomodoros.each do |pomodoro| %>
    <%= render pomodoro %>
    <p>
      <%= link_to "Details", pomodoro %>
    </p>
  <% end %>
</div>
